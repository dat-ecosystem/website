module.exports = context_api

let context = {}

function context_api(props){
    if(props.set_methods){
        Object.assign(context, props.set_methods)
    }
    if(props.get_methods)
        return Object.fromEntries(props.get_methods.map(key => [key, context[key]]))
}